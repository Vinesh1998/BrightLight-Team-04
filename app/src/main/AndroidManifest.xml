<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.app.brightLightBookStore">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        tools:targetApi="31">

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.BrightLight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <!-- User activities -->
        <activity android:name=".activities.CreateAccountActivity" />
        <activity android:name=".activities.User.UserProfile" />
        <activity android:name=".activities.User.BookDetails" />
        <activity android:name=".activities.User.BooksCart" />
        <activity android:name=".activities.User.Notifications" />
        <activity android:name=".activities.User.PaymentStatus" />
        <activity android:name=".activities.User.PaymentActivity" />
        <activity android:name=".activities.User.UserHistory" />
        <activity android:name=".activities.User.SearchBooksActivity" />
        <activity android:name=".activities.User.ChangePassword" />
        <activity android:name=".activities.User.ChatConversation" />
        <activity
            android:name=".activities.User.DashboardUserActivity"
            android:theme="@style/Theme.BrightLight.NoActionBar" />
        <!-- admin activities-->
        <activity
            android:name=".activities.Admin.DashboardAdminActivity"
            android:theme="@style/Theme.BrightLight.NoActionBar" />
        <activity android:name=".activities.Admin.AdminChatConversation"/>
        <activity android:name=".activities.Admin.AdminChats"/>
<!--        <activity android:name=".activities.Admin.UserPurchases"/>-->
        <activity android:name=".activities.Admin.UserRentals"/>
        <activity android:name=".activities.Admin.BooksManagementActivity"/>

    </application>

</manifest>